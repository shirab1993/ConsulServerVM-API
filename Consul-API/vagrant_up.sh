#!/bin/bash
# Use Vagrantfile and run Vagrant commends that Build & Run the VM 
# Vagrantfile install docker
vagrant up
vagrant ssh

echo "#######################################################"
echo " vagrant up"
echo "#######################################################"

